# Cross-platform implementation staging note

This feature branch contains a temporary, self-removing source bootstrap so GitHub Actions can materialize and compile the reviewed macOS and Windows implementation. The bootstrap and encoded payload files are deleted by the installation job before the implementation commit is pushed.
